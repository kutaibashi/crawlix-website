# Keystatic CMS Configuration
# ============================

# Admin Authentication (REQUIRED in production)
# Protects /keystatic admin panel with HTTP Basic Auth
KEYSTATIC_ADMIN_USER=admin
KEYSTATIC_ADMIN_PASSWORD=your-secure-password-here

# Storage mode: 'local' (default) or 'github'
# - local: Content is stored in your local filesystem (for development)
# - github: Content is stored in GitHub (for production/collaboration)
KEYSTATIC_STORAGE=local

# GitHub Repository Configuration (required when KEYSTATIC_STORAGE=github)
# The repository where content will be stored
GITHUB_REPO_OWNER=your-org
GITHUB_REPO_NAME=seo-crawler

# GitHub OAuth App (required for GitHub mode)
# Create an OAuth App at: https://github.com/settings/developers
# Set the callback URL to: https://your-domain.com/api/keystatic/github/oauth/callback
KEYSTATIC_GITHUB_CLIENT_ID=your-client-id
KEYSTATIC_GITHUB_CLIENT_SECRET=your-client-secret

# Secret for signing Keystatic auth tokens (generate with: openssl rand -hex 32)
KEYSTATIC_SECRET=your-secret-key
